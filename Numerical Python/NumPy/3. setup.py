"""
Installation of NumPy:
I have Python and Pycharm.
From the pycharm: file->setting->project->intrepreter->click on '+'
    ->write numpy->lenked appear->click on install package.
Once NumPy is installed, import it in your applications by adding the import keyword.

"""
import numpy

arr = numpy.array([1, 2, 3, 4, 5])
print(arr)